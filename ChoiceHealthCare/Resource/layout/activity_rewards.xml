<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context="com.mindbowser.choicehealthcare.activities.RewardsActivity" >

    <include
        android:id="@+id/activity_appointment_header"
        android:layout_width="match_parent"
        android:layout_height="60dip"
        layout="@layout/layout_action_bar_header"
        android:background="@color/white" >
    </include>

    <ListView
        android:id="@+id/activity_rewards_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/activity_appointment_header"
        android:layout_marginTop="10dip"
        android:cacheColorHint="@null"
        android:divider="@null"
        android:dividerHeight="10dip"
        android:listSelector="@color/transparent"
        android:padding="5dip"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_no_rewards"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:padding="5dip"
        android:text="@string/msg_no_rewards"
        android:textAllCaps="true"
        android:textColor="@color/gray"
        android:textSize="@dimen/size_title" />

</RelativeLayout>